# Article III: Decision states

Decision state is explicit and finite:

- `proposed`
- `confirmed`
- `inconclusive`
- `rejected`
- `superseded`

`confirmed` with `confidence: unknown` is invalid.

An inconclusive result is a legitimate endpoint.
