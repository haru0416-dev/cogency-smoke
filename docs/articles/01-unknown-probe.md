# Article I: UNKNOWN — probe

Unknown is a valid state, not a failure.

If a claim cannot be justified, the record must say `unknown` and name the probe that would reduce the uncertainty.

No `Why` is valid without one of these:

- direct evidence
- calibrated reasoning marked `plausible`
- `UNKNOWN — probe: <next step>`
